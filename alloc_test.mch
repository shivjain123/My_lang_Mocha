function main(didLoad) -> null {
    var a: float[][] = alloc float[3][2];
    print("rows=" + str(a.rows) + " cols=" + str(a.cols), newLine=true);
    var b: float[][] = alloc float[2][2];
    print("rows=" + str(b.rows) + " cols=" + str(b.cols), newLine=true);
};